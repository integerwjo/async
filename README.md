# Asynchronous Runtime
A minimal asynchronous runtime built from scratch to understand how async/await works under the hood in Rust. Implements a basic task executor, a reactor for I/O event notification, and a simple waker system to drive futures to completion — without relying on Tokio or async-std.
